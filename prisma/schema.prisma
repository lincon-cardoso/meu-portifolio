generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id             String    @id @default(cuid())
  name           String?
  email          String    @unique
  emailVerified  DateTime?
  hashedPassword String?
  role           Role      @default(ADMIN) // 👈 ADICIONADO
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
}

enum Role {
  ADMIN
}

model Projeto {
  id          String   @id @default(cuid())
  titulo      String
  descricao   String
  category    String
  tecnologias String[]
  link        String?
  linkGithub  String?
  criadoEm    DateTime @default(now())
  usuarioId   String
  destaque    Boolean  @default(false)
}
